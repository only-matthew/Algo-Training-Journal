# 首页活力展示修复（2026-09-13）

针对本地截图中的统计卡错位和图表比例问题完成修复，未修改活力公式或训练源数据。

- 统计卡中活力数字原本嵌在标题的 `strong` 内，被后加载的 `.stat-card strong` 设置为块级大字。现改为题数下方独立辅助行，保留 `site-total-badge` 和 `site-vitality-badge` 数据入口。
- 原图把累计值独立归一化后叠加到每日值坐标轴，无法按纵轴读取。现在通过“每日活力 / 累计活力”切换展示，每次使用对应的单一刻度；填充区域与折线使用相同数据。
- 图表限制最大高度，减小点线重量，日期按实际天数间距绘制；移动端在图表内部横向滚动，保留可读刻度。
- 绘图拆到 `lib/vitality-chart.mjs`，HTML 生成可独立测试；`renderer.mjs` 保留调用与兼容导出。
- 去掉“越高说明练到了能力边界”的过强解释，明确活力为估算指标。

验证：项目完整检查通过（原有 317 + 4 项测试、语法检查、训练索引、165 条记录构建）；随后新增的 3 项图表测试通过，覆盖两种刻度、真实日期间距、空数据与非有限数值。最后一轮样式调整后重新构建成功。浏览器检查桌面及 390px 手机布局无页面横向溢出，每日/累计切换正常，读取到的浏览器错误日志为空。

尚未发布。原有早期活力接近零、完成质量未接入等算法和数据问题仍按 VITALITY-DESIGN.md 跟进。本记录更新了旧设计中“累计底座叠加每日折线”的展示方案。
