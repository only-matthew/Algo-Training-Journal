# 2026-07-21

## 题目

P1104

## 平台

洛谷

## 难度

普及-

## 收获

这题我第一次提交时把排序方向写反了。错误代码如下：

```cpp
#include<iostream>
#include<algorithm>
#include<cmath>
using namespace std;
const int MAXN = 5e3 + 5;
struct Person{
    int num, grades;
    const bool operator < (const Person &other){
        if (grades != other.grades) return grades > other.grades;
        else return num < other.num; // 按照成绩从大到小排序，1号即为第一大的；但分数相同时按照序号
    }
};
Person a[MAXN];
int main(){
    int n, m; cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> a[i].num >> a[i].grades;
    sort(a + 1, a + n + 1);
    int m_ = (int)(1.0 * m * 1.5); // 自动向下取整
    // 寻找同分
    int pointer = m_;
    while(true){
        if (a[m_].grades == a[pointer + 1].grades) pointer++;
        else break;
    }
    cout << a[m_].grades << " " << pointer << "\n";
    for (int i = 1; i <= pointer; i++){
        cout << a[i].num << " " << a[i].grades << "\n";
    }
    return 0;
}
```

## 题目

P5143

## 难度

普及-

## 收获

```cpp
#include<iostream>
#include<algorithm>
#include<cmath>
using namespace std;
struct Point{
    int x, y ,z;
    const bool operator < (const Point &other){
        return z < other.z;
    }
};
const int MAXN = 5e4 + 5;
Point a[MAXN];
int main(){
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i].x >> a[i].y >> a[i].z;
    sort (a + 1, a + n + 1);
    double res = 0;
    for (int i = 2; i <= n; i++){
        res += sqrt(pow(a[i].x - a[i-1].x,2) + pow(a[i].y - a[i - 1].y,2) + pow(a[i].z - a[i - 1].z, 2)); 
    }
    printf("%.3f", res);
    return 0;
}
```