{
  "name": "algo-training-journal",
  "version": "1.0.0",
  "private": true,
  "description": "队伍训练日志 - 热力图",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "generate": "node scripts/generate-data.js",
    "migrate:date-layout": "node scripts/migrate-date-layout.js",
    "test": "node --test",
    "check": "node --check app.js && node --check lib/journal-api.js && node --check lib/log-schema.mjs && node --check lib/render-safety.mjs && node --check workers/oauth.js && node --check scripts/generate-data.js && node --check scripts/migrate-date-layout.js && node --check scripts/migrate-logs.js && node --test && npm run generate"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "marked": "^18.0.7"
  }
}
