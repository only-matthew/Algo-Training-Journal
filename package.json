{
  "name": "algo-training-journal",
  "version": "1.0.0",
  "private": true,
  "description": "队伍训练日志 - 热力图",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "generate": "node scripts/generate-data.js",
    "build": "npm run generate",
    "backfill:updated-at": "node scripts/backfill-updated-at.js",
    "migrate:date-layout": "node scripts/migrate-date-layout.js",
    "test": "node --test",
    "check:syntax": "node scripts/check-syntax.mjs",
    "verify": "npm run check:syntax && npm test && npm run build",
    "check": "npm run verify"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "@noble/ed25519": "^3.1.0",
    "@noble/hashes": "^2.3.0",
    "cheerio": "^1.2.0",
    "marked": "^18.0.7"
  }
}
